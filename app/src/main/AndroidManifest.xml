<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".KitchenBoardApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep-link: kitchenboard://add?name=...&category=... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kitchenboard" android:host="add" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <receiver
            android:name=".calendar.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".calendar.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".immobilien.ImmobilienCheckReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kitchenboard.action.IMMOBILIEN_CHECK" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".wellness.WellnessCheckReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kitchenboard.action.WELLNESS_CHECK" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".tasks.TasksCheckReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kitchenboard.action.TASKS_CHECK" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".update.AutoUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kitchenboard.action.AUTO_UPDATE_CHECK" />
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".shopping.StoreGeofenceReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kitchenboard.action.STORE_GEOFENCE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
