<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Title row with feature request button -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/module_weather_light"
        android:paddingStart="@dimen/panel_padding"
        android:paddingEnd="@dimen/panel_padding"
        android:paddingTop="@dimen/spacing_normal"
        android:paddingBottom="@dimen/spacing_normal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/weather_title"
            android:textSize="@dimen/title_text_size"
            android:textStyle="bold"
            android:textColor="@color/module_weather"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"/>

        <ImageButton
            android:id="@+id/btn_feature_request"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@android:drawable/ic_btn_speak_now"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:contentDescription="@string/feature_request_hint"/>

    </RelativeLayout>

    <!-- Compact location bar: city label (tap to edit) + icon buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="@dimen/panel_padding"
        android:paddingEnd="4dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tv_city_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="@string/city_hint"
            android:singleLine="true"
            android:ellipsize="end"
            android:textSize="@dimen/small_text_size"
            android:textColor="@color/text_secondary"
            android:background="?attr/selectableItemBackground"
            android:paddingStart="0dp"
            android:paddingEnd="4dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"/>

        <Button
            android:id="@+id/btn_locate"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:text="📍"
            android:textSize="18sp"
            android:padding="6dp"
            android:contentDescription="@string/locate_me"/>

        <Button
            android:id="@+id/btn_refresh"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:text="🔄"
            android:textSize="18sp"
            android:padding="6dp"
            android:contentDescription="@string/refresh"/>

    </LinearLayout>

    <!-- ViewPager2 for weather sub-pages, with progress bar overlay.
         Wrapped in NestedScrollableHost so the outer ViewPager2 does not
         intercept horizontal swipes that belong to this inner pager.
         The ViewPager2 MUST be the first direct child of NestedScrollableHost
         so that canScrollHorizontally() is evaluated on the pager itself. -->
    <com.kitchenboard.NestedScrollableHost
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="@dimen/panel_padding"
        android:paddingEnd="@dimen/panel_padding">

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/weather_view_pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <ProgressBar
            android:id="@+id/progress_weather"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"/>

    </com.kitchenboard.NestedScrollableHost>

    <!-- Dot indicator for weather sub-pages -->
    <LinearLayout
        android:id="@+id/weather_dot_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:orientation="horizontal"
        android:paddingTop="4dp"
        android:paddingBottom="2dp"/>

</LinearLayout>

